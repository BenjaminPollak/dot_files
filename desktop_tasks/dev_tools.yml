- name: Install dev tools (ubuntu)
  apt:
    name:
      - "postgresql"
      - "sqlite3"
      - "make"
      - "zsh"
  tags:
    - ubuntu
    - dev_tools
- name: Install dev tools (mac)
  apt:
    name:
      - "postgresql@14"
      - "sqlite"
      - "make"
      - "zsh"
  tags:
    - mac
    - dev_tools
- name: change user shell to zsh
  become: yes
  user:
    name: "{{ the_user }}"
    shell: /usr/bin/zsh
  tags: ubuntu
